services:
  hockeybot:
    build: .
    ports:
      - "${AUTH_SERVER_PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      - DB_PATH=/data/hockeybot.db
    volumes:
      - db_data:/data
    restart: unless-stopped

volumes:
  db_data:
